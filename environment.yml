name: transaction-qa
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - streamlit
      - pandas
      - pdfplumber
      - langchain
      - langchain-openai
      - langchain-experimental
      - python-dotenv
      - python-dateutil
      - reportlab
